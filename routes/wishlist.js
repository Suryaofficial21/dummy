// import express from "express";
// import { authorizeRoles, isAuthenticatedUser } from "../middlewares/auth.js";
// import {addWishList, getWishList, removeFromWishlist} from '../controllers/wishListControllers.js'
// const router = express.Router();
// router.route("/wishlist").post(isAuthenticatedUser,addWishList).get(isAuthenticatedUser,getWishList).delete(isAuthenticatedUser,removeFromWishlist);
// export default router;
